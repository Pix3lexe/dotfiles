# How to add your own wallpapers

1. Move them into the `personal/` directory (must be `png`, `jpg` or `jpeg`)
2. Execute `~/.config/eww/scripts/thumbnail_generator.sh` to make them appear in the wallpaper changer
